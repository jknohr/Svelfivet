// Common type definitions shared across components
import type { XYPair, CSSColorString, Direction, Dimensions } from '../../components/Templates/Canvas/types';

export type {
    // Position and dimension types
    XYPair,
    Dimensions,
    
    // Style types
    CSSColorString,
    
    // Layout types
    Direction
};
